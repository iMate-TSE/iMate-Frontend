using iMate.Services;

namespace iMate
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            // Navigation.PushAsync(new MainPage());

        }
    }
}
